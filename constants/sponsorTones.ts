// AI Sponsor — one consistent brand accent (per the "AI Sponsor Update" handoff).
// Personas no longer carry their own color identity; the whole surface uses the
// teal brand accent. See [[constants/sponsors]].
export const BR_INK = '#2E6F6F';  // primaryDark — strokes, quote text, send fill, icon tints
export const BR_SOFT = '#D8E8E8'; // primarySoft — soft fills (avatar backing, switch, selected row)

// The three sponsors shown in the redesigned picker + switcher, in order.
export const SELECTION_SPONSOR_IDS = ['supportive', 'salty', 'grace'];
